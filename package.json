{
  "name": "crm-pipeline-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "uv run fastapi dev backend/main.py --host 127.0.0.1 --port 5173",
    "dev:static": "python3 -m http.server 5173",
    "test": "node --test"
  }
}
