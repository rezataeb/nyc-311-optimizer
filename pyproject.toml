[project]
name = "crm-pipeline-dashboard"
version = "0.1.0"
description = "Lightweight FastAPI and static CRM pipeline dashboard demo."
requires-python = ">=3.12"
dependencies = [
  "fastapi[standard]>=0.115.0"
]

[tool.uv]
package = false
